# OSSP

<git & github practice>

local : working directory + staging area + local repository
remote : remote repository

git clone
git add
git commit
git push
git pull
(pull = fetch + merge)
git fetch
git merge
git branch
